# config.yaml

# 1. Credentials (Users)
credentials:
  usernames:
    # Example User 1: Replace with your actual user details
      my_username: sialaichai
      email: sialaichai@gmail.com
      name: sialaichai
      # Password for this user must be hashed (see Step 3)
      password: '$2b$12$SWx65Sv8lNHRYcT5n/HF8uAMzFmeJtMeHHTwONbMsX.tQ6vIlv4oK'

# 2. Cookie Configuration (Persistence)
cookie:
  # A unique name for your application's cookie
  name: my_app_cookie
  # A random, complex string for signing the cookie (VERY IMPORTANT FOR SECURITY)
  key: 'a_very_secret_key_that_is_at_least_32_chars_long'
  # Days the user stays logged in
  expiry_days: 30

# 3. Pre-authorized (Optional)
preauthorized:
  # You can pre-authorize emails here if needed
  emails:
    - sialaichai@gmail.com
